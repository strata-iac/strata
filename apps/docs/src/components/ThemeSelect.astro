---
// Intentionally empty: replaces Starlight's built-in ThemeSelect component
// (https://starlight.astro.build/reference/overrides/#themeselect) with nothing,
// hiding the light/dark toggle. The docs site is always dark — enforced by
// ThemeProvider.astro which sets data-theme="dark" unconditionally.
---
