{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"buildCommand": "bun install --frozen-lockfile && bun run typecheck",
	"installCommand": "bun install --frozen-lockfile",
	"framework": null,
	"bunVersion": "1.x",
	"functions": {
		"apps/server/src/vercel.ts": {
			"maxDuration": 60
		}
	},
	"rewrites": [
		{ "source": "/api/:path(.*)", "destination": "/apps/server/src/vercel.ts" },
		{ "source": "/trpc/:path(.*)", "destination": "/apps/server/src/vercel.ts" },
		{ "source": "/healthz", "destination": "/apps/server/src/vercel.ts" }
	],
	"crons": [
		{
			"path": "/api/cron/gc",
			"schedule": "* * * * *"
		}
	]
}
