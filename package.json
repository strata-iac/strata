{
	"name": "strata",
	"private": true,
	"workspaces": ["packages/*", "apps/*"],
	"scripts": {
		"prepare": "husky",

		"dev": "bun run dev:deps && trap 'kill 0' EXIT && bun --watch apps/server/src/index.ts & bun run --cwd apps/ui dev & mise exec -- caddy run --config Caddyfile & wait",
		"dev:deps": "docker compose up -d && printf 'Waiting for postgres' && until docker compose exec -T postgres pg_isready -U strata -q 2>/dev/null; do printf '.'; sleep 1; done && echo ' ready'",
		"dev:down": "docker compose down -v",

		"check": "biome check . && bun run typecheck && bun test --recursive",
		"check:ui": "biome check apps/ui/ && bun run --cwd apps/ui typecheck",
		"typecheck": "tsc --build",
		"types:generate": "cd packages/types/tygo && go mod tidy && tygo generate && sed -i '' 's/export type UpdateStatus = typeof UpdateStatusSucceeded | typeof UpdateStatusFailed | typeof UpdateStatusCancelled;/export type UpdateStatus = string;/' ../src/pulumi.gen.ts",
		"e2e": "bun test e2e/",
		"check:all": "bun run check && bun run check:ui && bun run e2e",

		"db:generate": "drizzle-kit generate --config packages/db/drizzle.config.ts",
		"db:migrate": "drizzle-kit migrate --config packages/db/drizzle.config.ts",

		"docker:build": "docker build -t strata:dev .",
		"docker:cluster": "docker compose --profile cluster up --build",
		"docker:cluster:down": "docker compose --profile cluster down -v",

		"docs:dev": "cd docs && npm run dev",
		"docs:build": "cd docs && npm run build"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.9.4",
		"@types/bun": "^1.2.0",
		"drizzle-kit": "^0.30.0",
		"husky": "^9.1.7",
		"typescript": "~5.8.3"
	},
	"dependencies": {
		"@descope/node-sdk": "^1.10.0",
		"@hono/zod-validator": "^0.5.0",
		"@trpc/server": "^11.1.0",
		"drizzle-orm": "^0.39.0",
		"hono": "^4.7.0",
		"zod": "^3.24.0"
	}
}
